# 🔹 Node.js dependencies (Don't delete locally, but ignore in Git)
node_modules/
package-lock.json

# 🔹 Build files (Generated by npm run build, not needed in Git)
frontend/build/
backend/build/

/build/

# 🔹 Firebase environment files (Sensitive, should not be in Git)
.env
firebase-service-account.json

# 🔹 Firebase Hosting default files (Optional)
firebase.json

# 🔹 Logs and caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
